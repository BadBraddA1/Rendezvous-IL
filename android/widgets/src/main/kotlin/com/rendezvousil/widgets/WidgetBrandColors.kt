package com.rendezvousil.widgets

import androidx.compose.ui.graphics.Color
import androidx.glance.unit.ColorProvider

/** Matches iOS BrandWidgetColors and app [com.rendezvousil.app.theme.BrandColors]. */
object WidgetBrandColors {
    val Lake = ColorProvider(Color(0.22f, 0.55f, 0.52f))
    val LakeLight = ColorProvider(Color(0.88f, 0.95f, 0.94f))
    val Coral = ColorProvider(Color(0.78f, 0.45f, 0.32f))
    val Secondary = ColorProvider(Color(0.45f, 0.45f, 0.45f))
}
